# sprintplan.uk - Build Log

The Capstone artefact. Every working session ends with an entry, per the rules of engagement.
Entries are newest first. Be honest: friction and failure are the valuable material.

---

## Entry Template (copy for each session)

### YYYY-MM-DD - [Brief title]

- **Task brief**: what was directed
- **AI contribution**: what the AI proposed and produced (summarise, link commits)
- **Human contribution**: decisions made, corrections issued, code rejected and why
- **Friction**: where the AI went wrong, hallucinated, over-engineered, or needed steering
- **Verdict**: what worked, what you would direct differently next time
- **Time**: rough effort spent

---

## Entries

### 2026-06-15 - Productionise slice MP1 — client sync layer + open-link rooms (real client over the proven op loop)

- **Task brief**: docs/phase2-mp1-client-sync-brief.md - first productionise slice after the spike PASS. Wire plan's REAL client store to the proven server op loop so two browsers edit one open-link board live; prove the real client (dispatch + reducers + render) survives being round-tripped through an authoritative server. Server-authoritative, pessimistic client. Dual-mode: with no ?room param the local single-user app is untouched. Inherits the three director rulings made this session (free + accounts + unused charge-later seam; standalone systemd deploy; dual-mode coexistence). One slice, RoE cadence, TDD.
- **AI contribution**: Promoted `spike/` → `server/` (git mv; the 16 spike proof tests kept green as regression). Built `public/js/sync-client.js` TDD (5 unit tests RED-first against an injected fake transport): `createRoomStore` mirrors createStore's getState/dispatch/subscribe (so every view module is reused unchanged), pessimistic — a local dispatch SENDS an op and waits, the store mutates only on the server broadcast; applies 'state'/'op'/'nack' frames; `wsTransport` is the thin browser-WebSocket glue + a `close()` for teardown. Branched `main.js` at boot on `?room=`: room store via wsTransport, onNack→toast, SKIP autosave + the resume-prompt gate (R3); local path byte-for-byte unchanged. Added to the server: express.static serving + a seeded dev room. End-to-end Node integration test (2 tests) drives the REAL sync-client over a REAL socket against the REAL server — two stores converge on ADD/MOVE/EDIT, and a concurrent same-story edit yields one winner + one stale nack with both converging. Browser-verified with headless Chromium (two contexts): both pages render the seeded card; an edit in A appears in B; room mode leaves sprintplan:board untouched; local mode still autosaves after an edit; zero console errors. 170/170 unit+integration, drift + typecheck clean.
- **Human contribution**: Ruled the three gating director decisions at the follow-on gate (product model, deploy model, coexistence — all as recommended), approved the slice-1 scope and the branch/promote plan, and directed the build to proceed. (Closes on sign-off of this entry.)
- **Friction**: Low. Two self-inflicted snags, both caught by the gates: a `join` name collision (node:path `join` vs the local room-socket `join` helper) tripped a TDZ error the moment static-serving ran — fixed by aliasing the import to `pathJoin`; and the first browser check wrongly expected a bare local-mode load to autosave, when by design the store only persists on a dispatch (fixed the check, not a bug). The integration test also surfaced that the room store needed a `close()` or node never exits on the open sockets — a real teardown method the app wants anyway. The pessimistic model paid off exactly as predicted: there was no client-side rollback logic to get wrong.
- **Verdict**: The store's long-standing "multiplayer-insurance" dispatch seam held its promise — swapping createStore for a wire-backed store with the same interface meant render, drag, the card editor and every other view module worked over the network with zero view changes. Driving the e2e proof through the real sync-client module at the Node level (only the transport differs from the browser) gave a fast, non-flaky convergence test; the browser run then confirmed the DOM and the dual-mode localStorage guarantee. Next slices: company-only rooms + real @suite/auth-client (2), per-entity rev + EDIT_STORY field-delta (3), room lifecycle/import UI + presence (4), systemd deploy (5).
- **Time**: ~1 session.

### 2026-06-15 - Phase 2 multiplayer SPIKE built — GO/NO-GO proof (PASS)

- **Task brief**: docs/phase2-multiplayer-spike-build-brief.md - the BUILD half of P2.1, written against the approved PROPOSE. Stand up the thinnest authoritative-room + serialized-op service (ws + better-sqlite3, company-scoped, auth-gated upgrade) and prove the zero-data-loss guarantee crosses the network on plan's ordered/interdependent/versioned document. Deliver a GO/NO-GO verdict against seven asserted cases, plus a written finding. Spike code is throwaway-tolerant; the finding is the durable artefact.
- **AI contribution**: Built the spike as five net-new files under `spike/` + `tests/`, reusing plan's shipped `reduce` and `validatePlan` BY IMPORT only (zero edits to the shipped client, per the isolation ruling). TDD throughout — two red-green cycles: the op-loop conflict proof (cases 1–6 + a vocabulary guard) driven directly against a real SQLite store, then the ws integration (case 7 auth/company boundary + transport/broadcast + the wire-level EDIT_STORY clobber from two real clients). All seven cases PASS; 16 spike tests green; full suite 163/163, drift + typecheck clean. Wrote docs/phase2-multiplayer-spike-finding.md (verdict, the one net-new rule, the honest tradeoffs, the productionise recommendation) and this entry.
- **Human contribution**: Ruled the two seam forks at the PROPOSE gate that shaped the build — concurrency = version-stamped optimistic rejection (not arrival-order-alone), and settings ops parked (structural-only op set). Directed the build to proceed and chose to hold the build brief on `spike-p2-multiplayer`.
- **Friction**: The model barely fought back — because plan's reducer + validatePlan were already the right shape, the net-new conflict surface collapsed to a single `if (baseVersion !== room.version)` gate. The one genuine wrinkle was conceptual and caught in the PROPOSE, not the code: `EDIT_STORY` is the lone whole-record-replace action, so it needs the version-gate where the delta-merging `EDIT_EPIC` does not. Global-version gating introduces a safe-but-blunt false-conflict (a stale edit rejects even when the intervening op touched a different story); recorded with per-entity `rev` as the production refinement. Also reconciled "upgrade without a session is refused" against open-link by scoping the session requirement to company-only rooms.
- **Verdict**: TDD was the right discipline precisely because the seven cases ARE the spec — every case passed on first implementation because the interpretation work lived in the tests. Driving cases 1–6 directly through the op loop (rather than only over ws) removed network nondeterminism from the conflict proof and made it crisp; the ws layer then proved transport/auth/broadcast separately. PASS unlocks a productionise feature brief; the deploy-inversion and coexistence rulings remain David's before it starts.
- **Time**: ~1 session.

### 2026-06-13 - Brief 9 built: Exports — markdown, printable HTML, CSV (P0 #6) — ALL P0 SCOPE COMPLETE

- **Task brief**: docs/brief9.md - give the facilitator the one-click shareable summary the whole tool exists to produce. From the live board, render the plan in three formats (markdown, self-contained printable HTML, flat CSV), each carrying the plan title, dated/partial sprints, per-sprint stories with points, the capacity status, a dedicated OVER-COMMITMENT section, and a DEPENDENCY-WARNINGS section. A PURE RENDER OF STATE: one pure model selector + three pure string renderers + thin download glue. No new action, no schema change, no store change; the report dispatches nothing and never writes the autosave envelope (R1/R2). Plus the R8 deps shape-guard. RoE: PROPOSE before building; branch `brief-9-exports` off main after PR #6 merged.
- **AI contribution**:
  - **Re-review of the revised brief, then PROPOSE-then-build** (per RoE): the brief had two findings outstanding from the prior review (HIGH: escaping specified for CSV only, silent on HTML/Markdown; MEDIUM: title-vs-summary inconsistency) plus a LOW suggestion (a pure-layer no-mutation assert). Confirmed all three were folded into the committed brief (`c6ca003`): a per-renderer escaping constraint (HTML `& < > "`; Markdown pipe/asterisk/underscore/backtick; CSV RFC-4180) with an asserted case per renderer (6/7/8); R6 resolving summary to its own CSV column while markdown/HTML show title+points only; and asserted case 10 (deep-freeze no-mutation). Grounded every load-bearing claim against the live source before any feature code: `planSummary` returns `{ months, sprints, stories, placedPoints }` only — **no capacity, no dates** — so the model derives `totalCapacity` (Σ `sprintCapacity`) and start/end dates itself; sprint objects already carry `startDate`/`endDate`/`isPartial`/`days` (generateSprints), so per-sprint dates and the partial flag are reads; `story.summary` exists (actions.js:61/63), the fact the R6 resolution rests on; and the R8 target is exact (`plan-io.js:72` enters the deps loop and dereferences `d.blockerId` with no object guard, `isObject` already in scope at line 19). Three build decisions approved as recommended: **HTML as a self-contained `.html` download** (inline `<style>`, no external asset, Ctrl+P-printable) over a live print view — it is the shareable artifact and doubles as the print path, and stays a pure string the unit net covers; the **report control as a third "Export summary ▾" `<details>` menu** in the topbar, in its own group separated from Download/Import board (R3/G8); and the **CSV including backlog rows** (sprint cell "Backlog") with the 8 columns epic/story/summary/points/sprint/sprint-capacity/sprint-status/dependency, so nothing the facilitator entered is silently dropped.
  - **Build (TDD, pure-first, red-green)**: the pure core watched fail before implementation (RED was the missing `report.js` module). New `public/js/report.js`, all DOM-free: `reportModel(state)` assembles `{ header, sprints, backlog, overCommitment, warnings }` reusing the established authorities — `planSummary`, `sprintPlacedPoints`, `sprintCapacity`, `pillState`, `overBy`, `isViolation`, `locationLabel`, `depLabel`, `storyLocation` — and holds RAW user strings (escaping is each renderer's job); plus three pure string renderers `toMarkdown`/`toHtml`/`toCsv`. Escaping is **per-renderer** and lives in the renderers: `mdEscape` (backslash first, then pipe/asterisk/underscore/backtick), `htmlEscape` (ampersand first, then `< > "`), `csvField` (RFC-4180 — wrap on comma/quote/newline, double inner quotes). The over-commitment and warnings sections each say so explicitly when empty (R4/R5). +16 unit tests in new `tests/report.test.js` pinning all of asserted cases 1–8 and 10 with a real plan state (titled; an over-capacity sprint 21/18; a prorated partial final sprint cap 12; exactly one violating pair amid a correct-order pair and a backlog-touching pair; a backlog story; a no-epic story; one story whose TITLE carries every format-hostile character and whose SUMMARY adds a newline) — the escaping cases assert the exact escaped substrings (md `\|`/`\*`/the escaped backtick; html `&lt;E&gt;`/`&amp;`/`&quot;` and that raw `<E>` never appears; csv the fully-quoted comma/quote/newline fields), and case 10 deep-freezes the fixture and asserts byte-identical state after model + all three renderers.
  - **R8 shape-guard (case 9)**: test-first in `tests/plan-io.test.js` — `deps: [null]` was confirmed to THROW today (`assert.doesNotThrow` went RED with "Got unwanted exception"); the one-line `if (!isObject(d)) return { ok: false, reason }` at the top of the deps loop turned it green. The only `validatePlan` change; it makes a throw into a clean boundary failure.
  - **Browser glue (the only DOM, browser-verified — the same boundary every brief kept its DOM on)**: `downloadText` generalised with a `mime` param (the board callers keep the JSON default; the report passes `text/markdown`/`text/html`/`text/csv`). A distinct `#tb-report` "Export summary ▾" `<details>` menu in index.html (its own topbar group, a thin `.tbacts-sep` divider from Download/Import board); on a pick, `exportReport` runs `reportModel(store.getState())`, renders the chosen format, and Blob-downloads it — a PURE READ that dispatches nothing. plan.css adds the dropdown (plum accent + `:focus-visible` rings, native `<details>` toggle, no JS for open/close) and the divider.
  - **Verification**: 146/146 unit tests (129 → 146: +16 report, +1 R8); `tsc --noEmit` clean. Headless Chromium against the export journey, **zero console errors**: a seeded populated board was restored via the resume prompt, then all three formats downloaded with the right filenames/extensions; the content reflects the live board (title "Q3 Roadmap", the over-commitment section lists Sprint 1, the warnings section lists D1/Login, markdown escapes the pipe, HTML is self-contained with entity-escaped angle brackets, CSV carries the backlog row and the RFC-4180-quoted hostile title); and — the data-loss bar (R2) — the `sprintplan:board` autosave envelope was captured before and after all three exports and was **byte-identical**, proving the export dispatches nothing and touches no persistence. A screenshot confirmed the menu renders visibly and distinct from the board file controls, over the over-committed board with the dependency-violation red treatment.
- **Human contribution**: Merged PR #6 (`--merge`, SHAs preserved) and authorised the build off main; reviewed the PROPOSE and approved the three build decisions as recommended (HTML self-contained download; the third topbar menu; CSV includes backlog rows). (Closes on the director's edit/sign-off of this entry.)
- **Friction**: Very low. The inherited atoms (overBy from Brief 4, isViolation from Brief 7/8, the capacity maths) meant the model was assembly, not new maths, exactly as the brief promised. The one design subtlety the build surfaced: the CSV "dependency" column needed per-story violation knowledge that the brief's sketched model shape didn't carry, so each story row in the model gained an `inViolation` flag (reusing isViolation, so it can never disagree with the warnings section) — a refinement within the approved PROPOSE, not a contract change. No load-bearing claim in the brief failed against the tree.
- **Verdict**: Holding escaping at the renderer boundary (the model holds raw strings; each renderer escapes its own format) is what made the three formats safe without a shared escape that would be wrong for at least two of them — the HIGH finding from review, now closed with a test per renderer. The deep-freeze case (10) defends the data-loss bar at the cheapest layer, and the browser run proved it where it matters: an export cannot mutate the saved board because it never dispatches and never writes. Reusing overBy and isViolation rather than re-deriving them means the report can never disagree with the board's pills and red treatment. **With Exports shipped, ALL P0 scope is complete — this is the MVP.**
- **Handoff: MVP LAUNCH (not a feature brief)**: the natural next step per the brief's suggestion is the closing retrospective (RoE), a README stub with the pitch and links to the spec and build log, and a launch checklist (licence — already AGPL-3.0 in package.json; no secrets in history; security alerts). After launch, the P1 fast-follow list: the stretch toggle and its report section (the report already counts every placed story honestly; a separate stretch listing is the fast-follow), contextual tooltips, the parked lane, labels/colour tags, the stats strip, and dark mode.
- **Open housekeeping** (carried, not actioned): promote `--plum`/`--plumwash`, `#glyph-plan`, the epic-palette tokens AND `--dep-line` to shared instrument-core; register `plan` in the theme `manifest.mjs` + drift test (suite-level). dragula vendoring decision stands (keep per-app copies). Doc/code naming note from Brief 6 still stands: `extractPlan`'s upload mode is `"file"` in code. The directional duplicate check in validatePlan stays as is (a reverse pair is a deferred 2-cycle surfaced as honest mutual violation flags, not a duplicate) — recorded as a deliberate non-change.
- **Time**: ~1 session.
- **Director sign-off** (closed 13 Jun 2026): All good. Exports landed as the pure render-of-state the brief promised — model selector plus three renderers, escaping held at each renderer boundary, the report dispatching nothing and leaving the autosave envelope byte-identical (the data-loss bar, proven in the browser). The three build calls (self-contained HTML download; third topbar menu; CSV carries backlog rows) were approved as recommended and shipped as built; the R8 shape-guard turned a throw into a clean boundary failure. "I can explain every line" holds. With this, all P0 scope is complete — MVP. No corrections issued.

### 2026-06-13 - Brief 8 built: dependencies — board drawing layer (P0 #5, slice 2 of 2)

- **Task brief**: docs/brief8.md - give the board its first drawn layer. Render the slice-1 dependency model directly on the vertical-stack board: an always-visible tether for a same-sprint pair, a cross-sprint connector on hover, and a board-side RED treatment on the card, the D badge and the connector when a pair is a violation. A PURE VIEW over the slice-1 model — it consumes `deps`, `isViolation`, `storyLocation` and the violation flag already carried on `depBadges`; adds one pure selector and one view module; dispatches nothing and adds **no action and no schema change** (R1/R2). RoE: PROPOSE before building; branch `brief-8-dependencies-drawing` off main after PR #5 merged.
- **AI contribution**:
  - **Brief review + PROPOSE-then-build** (per RoE): reviewed brief8.md against the live source before any feature code, and verified every load-bearing claim in the tree: `board.replaceChildren()` on every render (render.js:67); `el.style.gridRow = sprint.index + 1` and the `48px 1fr` grid; `data-story` on every card (`row.dataset.story`) and `data-sprint-index` on every `.sprint-body`; `.board`/`.workspace` have no `overflow` (the page scrolls, not the board); `dep-selectors.js` exports `isViolation`/`storyLocation`/`depBadges` (violation flag carried); schema v2, 17 actions. **One factual correction issued against the brief**: it placed the drag lifecycle in `main.js` (R7, WIRING), but the dragula instance and its `drag`/`dragend`/`drop` handlers live in `drag.js:44–71` (`main.js` only calls `setupDrag()` after render) — so the R7 hooks went into `drag.js`, and "redraw on drop" was found NOT free (a *cancelled* drag fires `dragend` with no dispatch and no render, leaving the layer hidden), so the redraw is hooked on `dragend`, not left to the drop's render. Nine decisions signed off as recommended ("Proceed - Signed off"), the two flagged hardest: **(D6) the at-rest red** — the brief's asserted case bundled "connector and both cards red" as one hover-gated sentence, but the GOAL ("turns red on the board, not only in the editor") and R8 (badge always visible) require the **card border + badge red at rest**; resolved so the card/badge red renders at rest (driven by the violation flag *already carried* in the `badges` array, so **no `storyCard` signature change**, R2) and only the connector *line* is hover-gated; and the resolution is **backlog-safe by construction** — `isViolation` is false whenever an endpoint is in the backlog, so a backlog card's badges/border never light up (R3/G7 for free). **(D3) hover-only, no click-pin** — click is already taken by `edit-story` (and swallowed by the drag), so overloading it would fight both; tethers always visible, cross-sprint connectors on hover.
  - **Build (TDD, pure-first, red-green)**: the pure core watched fail before implementation. New `connectorsToDraw(state)` in `dep-selectors.js` (alongside the slice-1 selectors): one entry per pair with BOTH endpoints placed (`{ dep, blockerId, blockedId, fromIndex, toIndex, kind, violation }`), `kind` = "tether" when `fromIndex === toIndex` else "connector", `fromIndex` the blocker's (do-first) sprint index, `toIndex` the blocked's, pairs touching the backlog excluded (R3). +5 unit tests in `tests/dep-selectors.test.js` (124 → 129) pinning the brief's five asserted cases: both-in-sprints draws a connector, same-sprint is a tether, backlog either side draws nothing, the violation flag matches `isViolation` across order/same-sprint/backlog, and direction/order (blocker/blocked ids + indices) resolve for the arrowhead. RED was the missing export; one tsc fix pinned the `kind` literal union.
  - **View module (browser-verified, not unit-tested — the same DOM boundary Brief 3's drag and Brief 6/7's prompt/picker sit on)**: new `connectors.js` owns one `<svg>` appended as the LAST child of `#board` (set `position: relative`), recreated each render (the board is torn down by `replaceChildren`, so endpoints are re-queried by `[data-story]` after layout and element refs are never cached). A single post-layout measure pass reads each endpoint's `getBoundingClientRect()` minus the `#board` box, so coordinates are board-local and scroll-independent (the page scrolls, there is no internal scrollport and no zoom). Tethers and cross-sprint connectors are right-gutter cubic bows (cards sit ~24px inside the sprint's right padding — real room); a shared arrowhead marker (`fill: context-stroke`) sits at the blocked/dependent end and follows the stroke colour. Module-level `hoveredStoryId` is view state held OUTSIDE the store (R6), driven by ONE delegated `mouseover`/`mouseleave` listener attached once to `#board` (which survives `replaceChildren`); a hover redraws ONLY the SVG, never the board.
  - **Wiring**: `render.js` sets `#board { position: relative }` and calls `drawConnectors(state)` as renderBoard's last step, after every card is in the DOM. `drag.js` hides the layer on `d.on("drag")` and redraws on `d.on("dragend")` (covers both drop and cancel). `backlog.js` `storyCard` adds `.bl-story--dep-violation` (any carried `badge.violation`) and `.dep-badge--violation` per badge — reading the flag it is **already handed**, so no signature change. `plan.css` adds a dedicated neutral `--dep-line` token (soft line, never plum, never amber/red), `.dep-layer` (absolute, `pointer-events: none`), `.dep-line`/`.dep-line--violation`, and the card/badge violation modifiers; the header note now records that `--red` reads in TWO non-overlapping places (capacity pill/banner AND dependency violation), never on the same element (R5).
  - **Verification**: 129/129 unit tests; `tsc --noEmit` clean. Headless Chromium, **22 checks, zero console errors**, every brief scenario: a same-sprint tether is visible with no interaction; cross-sprint connectors are hidden at rest and appear on hover, then hide on leave (R4); a violating pair shows red on BOTH cards and BOTH D badges **at rest** with the capacity pill unaffected (R5), and its connector is red on hover; a real dragula drag **hides the layer mid-drag and redraws on drop** (R7), the dragged dependent lands in the target sprint, and moving it after its blocker **clears the red** (out-of-order → in-order flips on the next render); a correct-order pair draws no red and a neutral connector on hover; a backlog-side pair draws no connector even on hover and keeps a neutral backlog badge (G7/R3); deleting a tethered story via the card editor removes its tether with the pruned pair; and a screenshot confirmed the gutter routing, the arrowhead at the dependent end, and the red treatment distinct from the neutral pills.
- **Human contribution**: Reviewed the brief and the PROPOSE; approved all nine decisions as recommended and authorised the build ("Proceed - Signed off"). (Closes on the director's edit/sign-off of this entry.)
- **Friction**: Low, but browser verification earned its keep with **one real bug it caught** (not a harness slip): the hover handler fought R7's hide-on-drag — because the pointer moves over cards *during* a drag, `mouseover` fired and called `paint()`, which re-showed the layer it had just hidden. Fixed with a module-level `suppressed` flag that `hideConnectors` sets and the hover handlers respect, cleared by the dragend redraw or the next full render; the "hidden during drag" check went from FAIL to PASS only after the fix. (Two earlier FAILs in the harness *were* genuine test artifacts — the "at rest" assertion fired while Playwright's pointer still sat over a card where the resume button had been, and the mid-drag snapshot raced dragula's async `drag` event; fixed by parking the pointer in a corner and polling.)
- **Verdict**: The slice stayed honest to "pure view" — one pure selector, one view module, two one-line wiring hooks, and a `storyCard` change that only reads a flag it was already passed. The load-bearing calls all held: deriving the card/badge red from the carried flag (not a `storyCard` signature change) kept R2 intact AND made backlog neutrality fall out for free (G7 means a backlog endpoint can never be a violation); routing the redraw on `dragend` rather than trusting the drop's render closed the cancelled-drag gap the brief's "redraw on drop" wording missed; and the `suppressed` flag is the small, correct answer to view-state-vs-gesture that the browser run forced into the open. With slice 2 done the dependency feature is complete end to end — model, editor, badge, board layer, board-side red — and **P0 #6 (Exports) inherits a finished `isViolation`** for dependency-violation warnings plus Brief 4's `overBy` for the over-commitment list.
- **Handoff to Brief 9 (Exports, P0 #6)**: the dependency feature is complete; Exports is the last untouched P0. It reuses `overBy` (Brief 4) for the over-commitment section and the now-complete `isViolation` (Brief 7) for dependency-violation warnings. Two slice-1 notes are the natural fit for that brief since it touches the I/O surface: `validatePlan`'s duplicate check is directional (a reverse pair is a deferred cycle, not a duplicate), and `validatePlan` does not shape-guard each `deps` element (`deps: [null]` throws rather than failing cleanly). Neither is a drawing concern; neither was bolted onto this slice.
- **Open housekeeping** (carried, not actioned): promote `--plum`/`--plumwash`, `#glyph-plan`, the epic-palette tokens AND the new `--dep-line` to shared instrument-core; register `plan` in the theme `manifest.mjs` + drift test (suite-level). dragula vendoring decision stands (keep per-app copies; no cross-repo move). Doc/code naming note from Brief 6 still stands: `extractPlan`'s upload mode is `"file"` in code.
- **Time**: ~1 session.
- **Director sign-off**: All Good (AI-drafted; closes on the director's edit/sign-off).

### 2026-06-13 - Brief 7 built: dependencies — data, picker, badge (P0 #5, slice 1 of 2)

- **Task brief**: docs/brief7.md - give the board a dependency data model and let a facilitator create/remove links from the card editor only (Journey 4, Screen 2). The heaviest remaining P0, split data-vs-drawing: this slice is the DATA (first schema bump since Brief 2, v1→v2, through the empty Brief 5 migrator seam) + the PICKER + the neutral shared D BADGE + the violation feedback *inside* the editor row. The board's drawn SVG layer (cross-sprint connectors, same-sprint tethers, board-side red) is slice 2 (Brief 8). RoE: PROPOSE before building; branch `brief-7-dependencies-data` off main.
- **AI contribution**:
  - **PROPOSE-then-build** (per RoE): answered the brief's closing PROPOSE list against the live source before any feature code, and verified every load-bearing claim in the tree first: `CURRENT_SCHEMA = 1` with `MIGRATORS = {}` an empty object and a real no-op `migratePlan` loop (the seam's intended first use, not a retrofit); `validatePlan` the single conservation spine; `card-editor.js` header stating the dependencies section is "intentionally absent"; `storyCard(story, epic)` the one shared renderer; 15 frozen action types; `exportPlan` stamps `schemaVersion` from `state.meta`. Eight decisions approved as recommended: **top-level `deps: [{ id, blockerId, blockedId }]`** (a first-class pair with its own id, so the shared D-badge identity is shared *by construction* — chosen over `story.dependsOn` which scatters the relationship and makes the shared number fiddly); `blockerId` = prerequisite/do-first, `blockedId` = dependent; the additive migrator + v2 seed in `createInitialState`; four new `validatePlan` reason strings (unknown-story names the id, self-dep, duplicate pair, non-array `deps`); a new pure `dep-selectors.js`; the D number **derived** from deps order (a within-session visual aid, not a stable reference); `LINK_DEP`/`UNLINK_DEP` with ids minted at the creator edge; delete-pruning in `deleteStory`/`deleteEpic`; an **inline expanding picker** (one modal at a time, simpler focus); the `storyCard` third `badges` param; and **defer true-cycle detection** (a cycle just yields mutual violation flags when both are scheduled — honest, and it never causes a real failure, so a DFS check earns nothing here).
  - **Build (TDD, pure-first, red-green)**: every pure unit watched fail before implementation. New `dep-selectors.js` (DOM-free): `storyLocation`/`locationLabel`/`depRole`/`depLabel`/`isViolation`/`depsForStory`/`depBadges`/`pickableDepTargets`. `isViolation` is clock-free and pure — true only when BOTH endpoints are in sprints AND the blocked story's sprint index is strictly less than the blocker's (R4/G7: backlog either side, same sprint, or correct order all neutral). New `tests/dep-selectors.test.js` (9 tests) pins location, role/label sharing (both endpoints read D1), the violation rule across order/same-sprint/backlog, the editor rows, the card badges, and picker exclusion. Schema: `CURRENT_SCHEMA = 2`, `MIGRATORS[1] = (plan) => ({ ...plan, deps: [], meta: { ...plan.meta, schemaVersion: 2 } })` (additive, never deletes), `createInitialState` seeds `deps: []` + v2, `validatePlan` learns the field. `tests/plan-io.test.js` updated for the bump (the newer-version guard now fires at v3, not v2; a v1 plan no longer passes through unchanged — it migrates) plus new cases 1–10. Actions/reducer: `LINK_DEP`/`UNLINK_DEP` (15 → 17 vocabulary), trivial reducer cases (append / filter by id), ids minted in `linkDep` at the edge. **Delete pruning (R7, the data-loss-critical line)**: `deleteStory` and `deleteEpic("delete")` filter `state.deps` in the same reducer step, so no delete can leave a dangling pair id — `validatePlan` would pass after any delete. New `tests/store-deps.test.js` (8 tests) pins the creators, both reducer cases, and pruning from both delete paths (with `validatePlan().ok` asserted after each). +33 unit tests (91→124 net; the deps suites plus the migrator/validation cases).
  - **DOM (browser-verified, not unit-tested — same boundary Brief 3/6 kept the gesture/prompt on)**: extended `card-editor.js` with a Dependencies section below Summary, shown only for a *saved* story (a new unsaved story has no id to link). Unlike the form fields (which commit on Save), the section commits **live** via `LINK_DEP`/`UNLINK_DEP` — a link is a fact about two existing stories, not a pending edit — and re-renders itself in place after each dispatch (the board/backlog re-render from their own store subscription). Existing links render as rows (shared badge, "needs"/"blocks", paired title, paired location in mono, a danger-ghost remove control); a violating row takes the red treatment with the location annotated ("Sprint 2, after this" / "before this", computed from the two sprint indices so it reads correctly from either endpoint). "This blocks…" / "This needs…" open the inline picker — a search input + `pickableDepTargets` grouped by sprint then Backlog, excluding self and already-paired; selecting dispatches `linkDep` with the current story on the chosen side and collapses the panel. `storyCard` gained an optional third `badges` param (least invasive); `render.js` and `backlog.js` compute `depBadges(state, story.id)` and pass it in, so the neutral D badge appears on both the board and backlog cards. plan.css adds the badge (plum/ink, neutral), the rows, the violation red treatment (the only red this slice; no capacity colours on the badge or picker), and the picker with plum `:focus-visible` rings.
  - **Verification**: 124/124 unit tests; `tsc --noEmit` clean. Headless Chromium, **26 checks, zero console errors**, every brief scenario: the section + both buttons render; the picker excludes self then also excludes the already-paired story and groups by location; selecting creates the pair, assigns D1, closes the picker, and the row appears with direction/title/location; the neutral D1 badge shows on both the board card and the backlog card; a "needs" pair where the dependent sits earlier renders the editor row red annotated "Sprint 2, after this" with no board-side red; the remove control drops the pair and the badge disappears from both cards; deleting a paired story prunes the pair (badge gone, `deps` empty, no dangling id); a duration shrink that returns a placed dependent to the backlog keeps the pair (badge persists, violation clears to neutral) — resolved decision 7, zero data loss; and a pre-Brief-7 **v1 save (no `deps`, schemaVersion 1) is offered for resume and migrates to v2 with `deps: []`** on the Brief 6 prompt path, no data loss.
- **Human contribution**: Reviewed the PROPOSE and approved all eight decisions as recommended (including the three flagged calls — field shape, derived D number, inline picker); authorised the build. (Closes on the director's edit/sign-off of this entry.)
- **Friction**: Low. The schema bump legitimately changed two existing `plan-io` tests' expected outcomes — the newer-version guard moves from v2 to v3, and a v1 plan now migrates rather than passing through unchanged — both restated by the brief's own asserted cases (4 and 2), so they were updated to the new expected values, not "fixed to pass". One mechanical test-harness slip caught in the browser run: `#ss-duration` is a `<select>`, so the duration-shrink step needed `selectOption`, not `fill` (verification code only; no product impact).
- **Verdict**: The field-shape call carried the slice — a first-class pair object with its own id made the shared-badge identity (D1 on both endpoints) fall out by construction, kept either-side creation to a single stored direction, made 1-to-many just "many pairs with the same blockerId", and reduced delete-pruning to one filter. Routing the bump through Brief 5's empty migrator seam made the first state-shape change since Brief 2 clean and additive: an old v1 autosave or file gains the empty field with no data loss, and `validatePlan` rejecting a dangling pair id at the load boundary keeps the zero-data-loss bar — the one unforgivable failure — intact. Keeping `isViolation` pure and wiring it only to the editor row this slice means Brief 8 (the board SVG) and P0 #6 (export warnings) inherit it ready-made. One feature per brief held: no SVG, no board-side red, no export section, no second link type.
- **Handoff to Brief 8 (slice 2)**: inherits the deps model, `isViolation`, `depBadges` (the violation flag is already carried on every badge, rendered neutral this slice), `storyLocation`, and the neutral badge — so slice 2 is purely the board SVG layer (cross-sprint connectors on hover/select, the always-visible same-sprint tether) plus the board-side red treatment on cards/connectors. No new action, no schema change.
- **Open housekeeping** (carried, not actioned): promote `--plum`/`--plumwash`, `#glyph-plan`, and the epic-palette tokens to shared instrument-core; register `plan` in the theme `manifest.mjs` + drift test (suite-level). dragula vendoring decision stands (keep per-app copies; no cross-repo move). Doc/code naming note from Brief 6 still stands: `extractPlan`'s upload mode is `"file"` in code.
- **Time**: ~1 session.
- **Director sign-off**: All Good (AI-drafted; closes on the director's edit/sign-off).

### 2026-06-13 - Brief 6 built: Resume / New-plan prompt, Screen 3 (P0 #7, slice 2 of 2)

- **Task brief**: docs/brief6.md - end silent restore and make starting fresh safe. On opening with a saved board present, show the Resume / New-plan prompt (Screen 3) instead of restoring silently; wire the dormant `NEW_PLAN` as the app's one destructive path with its download-first escape hatch; and close the Brief 5 R5 clobber by giving an unreadable save a visible, rescuable home. No new action type, state shape unchanged (schemaVersion stays 1); mostly the prompt view plus a bootstrap restructure over Brief 5's core. RoE: PROPOSE before building; branch `brief-6-resume-prompt` off main after Brief 5 merged.
- **AI contribution**:
  - **PROPOSE-then-build** (per RoE): answered the brief's closing PROPOSE list against the live source before any feature code. Grounded every claim in the tree: `loadOrInit` restored automatically with no prompt (main.js); the `{ savedAt, plan }` envelope is written but unread; `extractPlan`'s shipped mode is `"file" | "restore"` (the brief/handoff text said `"import"` - flagged the doc/code drift, used the shipped `"file"`); `modal.js` hard-wires its own Escape→`attemptClose` handler and autofocuses a field, so it cannot express the resume-vs-inert Escape split this prompt needs; `btn-pri` is the suite GREEN primary, so the brief's plum primary needed a scoped class. Five recommendations approved as proposed: dedicated `resume-prompt.js` shell (reuse `.modal-*` CSS, own the keydown); `relativeTime` 14-day bound → absolute ISO date; `planSummary` extracted into `board-selectors.js`; rescue = raw verbatim bytes; `downloadBoard`/`downloadRaw` stay in main.js.
  - **Build (TDD, pure-first, red-green)**: the two pure helpers first, DOM-free, every test watched fail before implementation. `relativeTime(fromISO, nowISO)` in date.js (clock-free: "now" is an argument) - the buckets seam the review flagged is resolved by making minutes/hours ELAPSED time but yesterday/N-days CALENDAR-day differences (via the existing `toOrdinal`/`parseISO`), so a sub-24h save that crossed midnight reads "yesterday", not "3 hours ago"; a missing/invalid timestamp returns "unknown" (the legacy-bare-save path), never throws. `planSummary(state)` in board-selectors.js reuses `sprintPlacedPoints` (no second points sum), with months from settings (not the sprint count) and stories = ALL stories (not placed-only). +10 unit tests (91 → 101) pinning all 9 asserted cases plus both sides of the hours→yesterday and 14-day boundaries.
  - **Bootstrap restructure (R2, the load-bearing call)**: replaced `loadOrInit` (which substituted the restored plan at boot) with `classifySave()` - it READS the stored bytes and returns `none` / `valid {plan, savedAt}` / `invalid {reason, raw}` but seeds nothing. The store now ALWAYS boots fresh (`createInitialState(nextMonday(todayISO()))`); a saved board is restored only by an explicit prompted `loadPlan` dispatch. Two safety properties fall out: the saved board never renders under the prompt (no last-quarter flash), and autosave (dispatch-only) cannot overwrite the saved bytes until the user chooses - which is exactly what keeps the R5 rescue available.
  - **Glue extraction (flagged refactor, not silent)**: lifted the inline `#tb-export` body into `downloadBoard(state)` (shared by the top-bar control and the Start-new escape hatch on the SAVED plan) plus a `downloadText` helper, and added `downloadRaw(text)` for the invalid-save rescue (raw verbatim, not reserialised). `#tb-export` now just calls `downloadBoard(store.getState())`.
  - **The prompt (`resume-prompt.js`, dedicated shell)**: pure view over `planSummary`/`relativeTime`; every side effect is a caller-supplied callback that dispatches an EXISTING action (`loadPlan`/`newPlan`) or runs the Brief 5 download/import glue - it never touches the store directly. Valid card: title (or "Untitled plan"), "Last edited [relative]", mono summary, plum primary Resume, danger-ghost Start-new that expands to a replace-warning + "Download current board first (.json)" + explicit confirm before `newPlan` is ever dispatched (R3), tertiary Import link reusing the hidden `#board-file`. Invalid variant (R5): "We couldn't read your saved board" + the plan-io reason, NO Resume, Start-new + Import + "Download the unreadable save (.json)". Escape/overlay-click RESUMES on the valid prompt, is INERT on the invalid one (a stray Escape can never drop the rescuable bytes). plan.css adds the prompt styles - plum primary + focus rings, danger-ghost Start-new, mono numbers, and a deliberately NEUTRAL invalid reason (amber/red are capacity colours and stay off the prompt).
  - **Verification**: 101/101 unit tests; `tsc --noEmit` clean. Headless Chromium, 36 checks, zero console errors, covering every brief scenario: no-save boots fresh with no prompt AND nothing autosaved (R2); a valid save shows the prompt with the saved title as heading while the board UNDER the overlay stays fresh (no last-quarter flash); Resume loads it; Escape on the valid prompt resumes; Start-new's "download first" downloads the saved board, then the board is fresh and the save is overwritten (a refresh shows the fresh board's OWN prompt, not last quarter's); Import from the prompt loads a file and closes it; an invalid save shows the reason, no Resume, downloads the RAW verbatim bytes on rescue, is NOT overwritten before the user acts, Escape is inert, and Start-new then overwrites - no console error throughout. Screenshots of both variants confirmed the branding.
- **Human contribution**: Reviewed the PROPOSE and approved all five recommendations as proposed; authorised the build. (Closes on the director's edit/sign-off of this entry.)
- **Friction**: Low, two honest notes. (1) Doc/code drift caught in grounding: the brief and the Brief 5 handoff both call `extractPlan`'s upload mode `"import"`, but the shipped code uses `"file"` - the build matched the shipped code rather than the prose, and this entry records it so the handoff isn't trusted over the tree next time. (2) `modal.js` could not be reused for the prompt: its fixed Escape handler routes through a dirty-guard and always closes, with no way to make Escape *resume* on one variant and be *inert* on another - so a small dedicated shell that reuses only the `.modal-*` CSS was the right call, confirmed in the browser (Escape resumes valid, inert invalid).
- **Verdict**: The whole slice turned on one restructure - boot fresh, classify the save, restore only by a prompted dispatch - and getting it right made three safety properties fall out together: silent restore is gone (Journey 6), the last-quarter render leak is closed at the render level (the board under the overlay is provably the fresh scaffold, not the saved plan), and the R5 clobber is closed because the untouched bytes stay rescuable until the user acts. Keeping the prompt a pure view that only dispatches existing actions held the 15-action vocabulary and the schemaVersion-1 shape unchanged - the dependencies schema bump stays wholly in Brief 7. P0 #7 (local persistence + file export/import) is now complete across Briefs 5 and 6.
- **R5 clobber: CLOSED.** Brief 5's known limitation (an invalid autosave silently discarded then overwritten) is closed here: an unreadable save is surfaced in the prompt with its reason and a raw-bytes rescue, and is not overwritten until the user starts-new or imports.
- **Open housekeeping** (carried, not actioned): promote `--plum`/`--plumwash`, `#glyph-plan`, and the epic-palette tokens to shared instrument-core; register `plan` in the theme `manifest.mjs` + drift test (suite-level). dragula vendoring decision stands (keep per-app copies; no cross-repo move).
- **Time**: ~1 session.
- **Director sign-off**: All Good (AI-drafted; closes on the director's edit/sign-off).

### 2026-06-12 - Brief 5 built: board file export and import (P0 #7, slice 1 of 2)

- **Task brief**: docs/brief5.md - make the board a file you can trust. Add the explicit file half of P0 #7 (download/import a .json) and, more importantly, the validation the existing load paths skip today. A single pure `validatePlan` spine every load path passes through; atomic import (validate before touching the board); a self-identifying file header; a `migratePlan` version seam; a localStorage `{ savedAt, plan }` envelope. No new action type, state shape unchanged (schemaVersion stays 1), NEW_PLAN stays dormant.
- **AI contribution**:
  - **PROPOSE-then-build** (per RoE): answered the brief's eight-item PROPOSE before any feature code, grounded in the live source. Verified every load-bearing claim against the tree first: `LOAD_PLAN` returns `action.payload` verbatim (store.js:197); `loadOrInit` returns `JSON.parse(localStorage)` straight as state with only a parse-error catch (main.js:42); both `NEW_PLAN`/`LOAD_PLAN` are real reducer cases never dispatched; autosave persists bare state; `.topbar` `.tbacts` exists for G8. Recommendations approved: a new `plan-io.js` (not a split of the editor-level `validate.js`); **extract the points rule into one authority** - `isValidPoints(n)` in validate.js, with `parsePoints` refactored to consume it and `validatePlan` reusing the same predicate, so no second points threshold can drift (the same discipline Brief 4 applied to `overBy` vs `pillState`); a version-loop migration seam over an empty `MIGRATORS` map (Brief 7 adds `MIGRATORS[1]` and bumps `CURRENT_SCHEMA`, additively); a single `extractPlan(parsed, mode)` with the leniency split (strict on uploaded files: require the `app` header; lenient on our own restore bytes: envelope OR legacy bare); `nowISO()` as a second boundary clock read alongside `todayISO()`, neither ever in state. Resolved the two flagged ⚑: case-9 (schema-version verdict) is owned by `migratePlan`, `validatePlan` checks structure/conservation only; and `btn-ghost`'s focus ring is the suite default (black), so plum had to be added (see Friction).
  - **Build (TDD, red-green-refactor)**: pure core first, DOM-free, every test watched fail before implementation. New `plan-io.js`: `validatePlan` (required keys + kinds; conservation - every backlog/placed id exists in stories and no id appears in two arrays, the invariant MOVE_STORY protects, now enforced at the load boundary; epicId null-or-existing; points via `isValidPoints`), `migratePlan` (missing/invalid or newer-than-build fails clearly; v1 passes), `exportPlan(state, exportedAt)` (R2 header, `lastReturnedStoryIds` reset to [] per R6, pure - caller supplies the clock), `extractPlan(parsed, mode)` (the leniency split). New `tests/plan-io.test.js`: 16 tests covering all 12 asserted cases with real fixtures (a fresh default plan and a `placedPlan()` built via `reduce`/`moveStory`), including the full conservation set (dangling backlog id, dangling placed id, duplicate id), the schema-version guard, the round-trip, and legacy-bare tolerance. Then the thin browser glue in main.js: `loadOrInit` now parses → `extractPlan("restore")` → `migratePlan` → `validatePlan` → `normalise` (lastReturnedStoryIds → []), falling back to a fresh plan on any structural failure exactly as a parse error already did (R5); the autosave subscriber writes `{ savedAt: nowISO(), plan: state }` (R7); a `tb-export` handler builds the file, Blobs it, and clicks a temporary `<a download>` (filename `slugify(title)-<date>.json`); a `tb-import` handler reads the file, runs the full pipeline, and only on success dispatches `loadPlan(normalise(plan))` - atomic, so a bad or foreign file leaves the board untouched (R3). index.html gains the two G8 buttons + a hidden file input in `.tbacts`. plan.css adds the plum hover/focus-visible ring for the two buttons.
  - **Verification**: 91/91 unit tests (75 → 91); `tsc --noEmit` clean. Headless Chromium (15 checks, zero console errors): both G8 buttons visible, file input hidden; autosave writes the `{ savedAt, plan }` envelope and savedAt is NOT in store state (R7); a legacy bare-state save (no envelope) still restores without crashing (R5); export produces a self-identifying `{ app:"sprintplan", schemaVersion, exportedAt, plan }` file with `lastReturnedStoryIds` reset (R2/R6); a foreign file shows "not a sprintplan board" and the board is byte-identical afterward (R3); a scratched board re-imports the exported file back to identical (R3 success path). Screenshot confirmed the plum focus ring on the buttons.
- **Human contribution**: Reviewed the PROPOSE, signed off ("all good"), authorised the build. Per RoE the approach (module home, the points-rule single authority, the migrate seam, the extractPlan leniency split, the boundary stamping, the G8 control) was approved before any feature code.
- **Friction**: Low, three honest notes. (1) The two ⚑ from the PROPOSE both resolved cleanly in code: `migratePlan` owns the version verdict so `validatePlan` stays a pure structural/conservation check; and the browser confirmed `btn-ghost`'s focus ring is the suite default black, so a scoped plum `:focus-visible` ring (plus plum hover) was added to plan.css for the two buttons - the brief's "plum accent for the buttons and focus rings" needed CSS, not just class reuse. (2) Two `tsc` snags in the new pure file, both fixed at source: a typed `Array<[string, fn]>` shape table confused inference (rewritten as two plain key loops), and the `isObject` const slid between `validatePlan`'s JSDoc and its declaration so the doc bound to the wrong symbol (const moved above the doc). (3) The Playwright seed hit the same fresh-load gotcha as Brief 3 - autosave fires on dispatch, not on init, so a fresh board isn't persisted until the first action; the seed nudges the title to force a save before reading localStorage. Pre-existing behaviour, unchanged by this brief (the envelope only changes the format of what a save writes, not when).
- **Verdict**: Building `validatePlan` as the single spine every load path passes through turned "restore is silently unsafe" from a finding into a closed gap: a dangling id, a duplicate id, a foreign file, or an unknown schema version now fails at the boundary with a named reason, not in the renderer, and the conservation invariant MOVE_STORY guards in memory is now enforced on every load too. Extracting `isValidPoints` as the one points authority (rather than re-deriving the rule in validatePlan) means a future drift fails the unit net, the same anti-dishonesty discipline Brief 4 used. The leniency split - strict where the user supplied the bytes, lenient where we wrote them - is the load-bearing safety call, and tolerating a legacy bare save is itself a no-data-loss decision (discarding a valid legacy board would be the loss we are here to prevent). One feature per brief held: the resume prompt, NEW_PLAN's destructive flow, report export, and dependencies all stayed out; NEW_PLAN remains dormant so no unguarded data-loss path ships.
- **Known limitation (R5, recorded)**: until Brief 6 gives a bad save a home in the resume prompt, a structurally-invalid autosave is discarded (fresh plan), not recovered, and the next action's autosave overwrites it (the clobber). This brief only stops the crash; Brief 6 closes it. **→ CLOSED in Brief 6 (see the 2026-06-13 entry above): the invalid save is now surfaced in the prompt with its reason and a raw-bytes rescue, and is not overwritten until the user acts.**
- **Open housekeeping** (carried, not actioned): promote `--plum`/`--plumwash`, `#glyph-plan`, and the epic-palette tokens to shared instrument-core; register `plan` in the theme `manifest.mjs` + drift test (suite-level). dragula vendoring decision stands (keep per-app copies; no cross-repo move).
- **Time**: ~1 session.
- **Director sign-off**: All Good (AI-drafted; closes on the director's edit/sign-off).

### 2026-06-12 - Brief 5 authored: board file export and import (pre-build)

- **Task brief**: Recover a stalled prior session, check the live dev environment for Brief 4's actual state, ingest the doc set, confirm the next-brief scope (P0 #7 re-sequenced ahead of dependencies), and author docs/brief5.md against the live code. Planning/brief session in the Claude app (Fable), not a Claude Code build session.
- **AI contribution**:
  - **Recovery and grounding**: the session opened on a stalled prior attempt ("I think you were stuck"). Re-established filesystem access and read the docs and source directly rather than walking the full repo tree, the likely cause of the stall (a broad \\wsl.localhost traversal over node_modules). Read spec v0.5, RoE, build-log, build-state-after-brief4, screen designs v1.0, and the live persistence source (store.js, main.js, actions.js) against each other, not from the handoff alone.
  - **Scope**: surfaced the next-brief fork (dependencies P0 #5 vs re-sequencing P0 #7) and recommended P0 #7 on the project's own quality bar: zero data loss is the one unforgivable failure, the silent-restore exposure is the real landmine, #7 is the lighter lift, and doing it first de-risks the SVG connector work in #5. Director chose #7. Then recommended splitting #7 into two briefs (5: board file I/O + validator; 6: resume prompt) to hold one-feature-per-brief, mirroring the deps 5a/5b split the handoff floated.
  - **The finding that drove the brief, confirmed against live source**: TWO unvalidated load paths. LOAD_PLAN returns action.payload verbatim (zero validation), and loadOrInit returns JSON.parse(localStorage) straight as state with only a parse-error catch, so a structurally-invalid-but-parseable object loads and crashes render. Both bypass every reducer invariant. Made a single pure validatePlan the spine every load path passes through, and made import atomic (validate first, a bad file never touches the board).
  - **Second real finding**: the Brief 6 resume card wants "last edited" but state carries no timestamp, so the brief introduces a localStorage envelope { savedAt, plan } now, stamped at the persistence boundary (never in state), so Brief 6 gets it free and the storage format changes once, not twice.
  - **Flagged seven judgement calls (R1-R7)** for ruling rather than baking: boundary validation with a pure reducer (R1); a self-identifying file header (R2); atomic import (R3); a version-dispatch migration seam built now though only v1 exists (R4); loader validates the restore with full recovery deferred to Brief 6 (R5); lastReturnedStoryIds normalised to [] on load (R6); the savedAt envelope introduced now (R7). Handed back hardest the two-envelope / extractPlan leniency split: strict on uploaded files (require the app header), lenient on our own legacy bare saves.
  - **Surfaced one scope consequence of the rulings**: NEW_PLAN must stay dormant this brief. A bare New-plan button is an unguarded data-loss path; the destructive start-new flow needs its download-first escape hatch, which lives in the Brief 6 resume prompt.
  - **Wrote brief5.md** in the established brief voice (READ FIRST, GOAL, WHAT THIS SLICE EXTENDS, THE FINDING, RULINGS, THE SUBTLE CALL, ARCHITECTURE CONSTRAINT, THE PURE CORE, WIRING, UI, OUT OF SCOPE, ASSERTED CASES, HOUSEKEEPING, BRANDING, DEFINITION OF DONE, PROPOSE-first close), with 12 asserted cases including the full conservation set (dangling and duplicate ids enforced at the load boundary), the schema-version guard, and the export round-trip.
- **Human contribution**: Directed the session (recover, check env, ingest, confirm scope, author Brief 5, draft this entry). Chose P0 #7 over dependencies. Ruled R1-R7 on Recommended in-session, which also closed the two-brief split. Proceeded to authoring without overruling the surfaced NEW_PLAN-dormant consequence (tacit acceptance).
- **Friction**: Low, with two honest notes. (1) The session began recovering from a stalled prior attempt; the lesson logged for reuse is that the container file tools (create_file/str_replace) write to the sandbox, not this WSL repo, and a "success" there does not land a file in /docs, while the Filesystem tools do write to the repo and scoped reads work where a broad tree walk hangs. brief5.md and this entry were (re)written via the Filesystem tools after a container-tool write silently missed the repo. (2) Unlike the Brief 3 and Brief 4 authoring sessions, the load-bearing claims here (LOAD_PLAN unvalidated, loadOrInit returns parsed JSON directly, both actions dormant, no timestamp in state) were confirmed by reading the live files this session, not taken from the handoff, so less is left for the build session to verify. What stays genuinely unproven until build: that the pure-core design lands as tidily in code as on paper (reuse of validate.js's points rule; the single extractPlan with its file-vs-restore leniency), and the rulings, though closed, ship provisional until the build confirms them. Process change worth naming: rulings were closed during authoring this time rather than in a later review pass, one fewer open gate than the Brief 2/3/4 authoring sessions carried.
- **Verdict**: Grounding the brief in the live persistence source, not the handoff, is again what made it credible: reading store.js and main.js turned "restore is silently unsafe" from an assertion into a confirmed two-path finding with file-level specifics, and it surfaced the missing timestamp before Brief 6 needs it. Recommending the #7-over-#5 re-sequence on the project's own zero-data-loss bar, not on which demo is flashier, is the kind of call the RoE wants the director making with the AI's reasoning on the table. One feature per brief held: the resume prompt, the report export, NEW_PLAN, and dependencies all stayed out even though import/export makes the prompt buildable. Splitting into Brief 5 and Brief 6 kept the slice small.
- **Director review**: All Good. (R1-R7 ruled Recommended in-session; the NEW_PLAN-dormant consequence surfaced and accepted by proceeding. Closes on the director's edit/sign-off of this entry.)
- **Time**: ~1 session (recovery, scope, and planning; no code).

### 2026-06-12 - Brief 4 built: capacity honesty banner (P0 #4)

- **Task brief**: docs/brief4.md - the honest surfacing of over-commitment. Brief 3 lit the pills off neutral, so a sprint can now be genuinely over capacity; this is the slim, dismissible banner that renders inside an over-capacity sprint, names the overshoot in points, and refuses the "stretch goal" rationalisation. Derived-view slice: no new store action, one pure helper, one view module, render wiring, one click branch, CSS.
- **AI contribution**:
  - **Review-then-propose** (per RoE): grounded the brief against the live code before building. Confirmed renderBoard already computes the three figures the banner needs per sprint (placed = sprintPlacedPoints, capacity = sprintCapacity, state2 = pillState at render.js:84-88); that `.sprint` is already `flex-direction:column; gap:12px` so the banner slots between head and body with no layout surgery; that the `#board` click listener matched only `closest("[data-act='edit-story']")` (so a dismiss button with a different data-act can't collide); and that the five settings-strip handlers are the only regenerating dispatch paths. The three flagged rulings were already closed in the authoring review (regeneration clears dismissed; amber/red-matched wash; copy "Over committed by N pts..." no hyphen), so the proposal covered implementation mechanics only. Recommended (all approved): overBy in plan-maths.js shipped bare (no combined {over,by} helper); a `dispatchSettings(action)` wrapper routing the five handlers through clearDismissedBanners()+dispatch rather than five inline clears.
  - **Build (TDD, red-green-refactor)**: pure helper first, test watched fail before implementation. `overBy(placed, capacity) = Math.max(0, placed - capacity)` in plan-maths.js next to pillState. Tests assert cases 1-6 with real values PLUS an exhaustive equivalence check - `overBy(p,c) > 0` is identical to `pillState(p,c) !== "neutral"` across 820 (placed,capacity) combinations - so a future second-threshold regression fails the unit net, not just review. Then the view: `banner.js` mirroring backlog.js's `collapsed` (module-local `dismissed` Set of sprint indexes; dismissBanner / isBannerDismissed / clearDismissedBanners; bannerEl builder with the ruled copy, mono N, and a × carrying data-act="dismiss-banner"). render.js appends the banner between head and body when overBy > 0 && !isBannerDismissed. main.js: the `dispatchSettings` clear-wrapper on the five settings handlers, and the `#board` listener widened to `closest("[data-act]")` + a switch (edit-story / dismiss-banner). CSS reuses the pill's exact --amberwash/--redwash/--red tokens matched to state, no new token, no plum (plum only on the dismiss focus ring, the app interaction accent).
  - **Verification**: 75/75 unit tests (72 -> 75); `tsc --noEmit` clean. Headless Chromium: red banner (21/18, "by 3", exact copy) and amber banner (19/18, "by 1") render; neutral (10/18) and empty sprints show none; dismiss hides that sprint's banner while the pill stays 21/18 and the other sprint's banner persists (per-sprint); reload re-arms; a settings change re-arms the dismissed banner; the × never opens the editor; a placed card still does; zero console errors.
- **Human contribution**: Reviewed the brief, signed off the proposal ("signed off - All good"), authorised the build. Per RoE the approach was approved before any feature code. (The three brief-internal rulings were already closed in the authoring review above.)
- **Friction**: Very low - the seams from Brief 3 made this nearly mechanical. The one genuine design choice (regeneration-clears mechanism, since store subscribers see state not the action) was resolved exactly as the brief's PROPOSE step anticipated: clear the view-local Set inside the five settings handlers via a small `dispatchSettings` wrapper, with MOVE_STORY and all other dispatches deliberately untouched so the per-session dismiss promise holds. No store contract widened.
- **Verdict**: Making "the banner can never disagree with the pill" an *exhaustively* tested invariant (820 combinations, not a handful of points) is the right altitude for the tool's own honesty surface - a drifting threshold would be precisely the dishonesty the app exists to prevent, and now it cannot ship. Reusing backlog.js's collapsed-Set pattern verbatim meant the dismiss-per-session requirement cost one small module, not a new mechanism. Holding the dragula dedupe to a raise-only decision note kept one-feature-per-brief intact. One feature per brief held: dependencies, exports, and the stretch toggle (the banner's natural companion) all stayed out.
- **Open housekeeping**: dragula-duplication decision note written (docs/housekeeping-dragula-vendoring.md) - recommendation is keep per-app copies; no files moved, no cross-repo change (RoE: the shared location is a second live app + suite infra outside this repo). Still carried, not actioned: promote `--plum`/`--plumwash`, `#glyph-plan`, epic-palette tokens to shared instrument-core; register `plan` in the theme `manifest.mjs` + drift test.
- **Time**: ~1 session.
- **Director sign-off** (closed 12 Jun 2026): All good. Both approved mechanics landed as built (overBy bare in plan-maths.js; the `dispatchSettings` clear-wrapper on the five settings handlers); "I can explain every line" holds. No corrections issued.

### 2026-06-12 - Brief 4 authored: capacity honesty banner (pre-build)

- **Task brief**: Review the build progress (docs/build-state-after-brief3.md), confirm Brief 4 scope, and author docs/brief4.md against the live code. Planning/brief session in the Claude app (Fable), not a Claude Code build session.
- **AI contribution**: Read the doc set and the actual source against each other (render.js, plan-maths.js, board-selectors.js, backlog.js, main.js, index.html, plan.css, package.json, server.js, spec v0.5, screen designs v1.0, RoE, build log, the Brief 3 handoff) rather than authoring from the handoff alone. Scoped Brief 4 to P0 #4 (capacity honesty banner) per the handoff: Brief 3 lit the pills off neutral, so a sprint can now be genuinely over capacity and the banner is meaningful for the first time. Made the brief's spine an invariant: **the banner must never disagree with the pill**. Visibility is derived from the existing tested pillState (not a parallel threshold), the overshoot is one pure tested helper (overBy = max(0, placed - capacity)), and asserted case 1 pins the boundary (placed == capacity then capacity + 1) so a separate or red-only threshold fails the test. Confirmed against the live code that this needs **no new store action**: renderBoard already computes placed (sprintPlacedPoints), capacity (sprintCapacity), and state2 (pillState) per sprint, so the banner is pure derived view data; and the dismiss-per-session pattern has a clean precedent in backlog.js's module-local `collapsed` Set plus main.js's paint-on-toggle, with the existing isDragging-guarded #board click listener a home for the dismiss click. Flagged seven rulings for veto rather than baking silently (visibility-from-pill; no-new-action; dismiss-view-local-and-unpersisted; pill-is-always-on-banner-is-one-time, so a dismissed banner does not re-nudge on further mid-session drags because the pill already carries the truth; banner uses the capacity wash matched to pill state, never plum; copy taken from the screen design because it carries the number; partial sprints banner too on prorated capacity). Flagged hardest: **regeneration clears the dismissed set**, which closes a stale-index hole (a re-grown sprint at a previously-dismissed index would otherwise suppress a real over-commit), with the clearing mechanism deliberately left to the build's PROPOSE step because store subscribers see only state, not the action. Scoped the dragula-duplication housekeeping to **raise a decision-ready ticket only, not execute the cross-repo move**, because de-duplicating touches retrospective (a second live app) and the shared suite serving setup outside this repo (RoE: no silent cross-repo refactor). Also verified, in answer to a director question, that there is **no database** in v1: package.json carries only Express, server.js is a static host, plan state is localStorage plus JSON export, and the housekeeping tickets are markdown in the log and handoffs, not a tracker or DB.
- **Human contribution**: Directed the session (review build progress, confirm scope, author Brief 4, draft this entry). Confirmed Brief 4 = banner only, with the dragula dedupe ticket riding along and the dismiss-per-session view-local pattern. Accepted the raise-only scoping for the dragula ticket ("your recommendation"). The three brief-internal calls (regeneration-clears-dismissed, amber/red-matched banner wash, exact copy and hyphen) left for the review pass.
- **Friction**: Low, same honest caveat as the Brief 2 and Brief 3 authoring entries: this logs an authored artefact whose flagged rulings are the director's call and ship provisional until a review pass closes them, with regeneration-clears-dismissed handed back hardest. No code or tests ran this session, so the load-bearing claims ("no new store action", "the banner derives from existing placed/capacity/state2", "backlog.js's collapsed Set is a faithful dismiss precedent") were grounded by reading the live files but must be confirmed by the build session before being treated as fact, the same discipline that confirmed Brief 3's count-vs-points finding at build time. The regeneration-clears mechanism is genuinely unresolved (subscribers receive state only) and was handed to PROPOSE rather than guessed.
- **Verdict**: Grounding the brief in the live source paid off again: reading renderBoard and confirming placed/capacity/state2 are already computed there is what let the brief credibly promise "no new store action" instead of hoping for it, and finding backlog.js's collapsed Set turned the dismiss-per-session requirement into a copy of an existing blessed pattern rather than a new invention. Making "the banner must never disagree with the pill" an asserted invariant (boundary at capacity then capacity + 1) applies the RoE danger-zone discipline to the tool's own honesty surface, where a drifting threshold would be exactly the dishonesty the app exists to prevent. Holding the dragula dedupe to raise-a-ticket-only kept one-feature-per-brief intact and stopped a cross-repo refactor sliding in under a feature brief. One feature per brief held: dependencies, exports, and the JSON-persistence half of P0 #7 all stayed out, even though the data-loss gap is the more tempting target and was named as the next priority after this banner.
- **Director review** (closed 12 Jun 2026): All three flagged rulings ruled as recommended. Regeneration clears the dismissed set (kept, not overturned to backlog-collapse parity). Banner wash is amber/red-matched to the pill. Copy is the screen-design string "Over committed by N pts. Relabelling it a stretch goal does not add capacity." with no hyphen in "Over committed". Scoping confirmed: dragula dedupe is raise-only. No corrections issued; brief4.md goes to Claude Code as written.
- **Time**: ~1 session (review and planning, no code).

### 2026-06-12 - Brief 3 built: drag-and-drop placement (P0 #3)

- **Task brief**: docs/brief3.md - the keystone slice. Backlog story cards become draggable into sprint containers, between sprints, and back to the backlog; the capacity pill moves off neutral for the first time on real placed totals. Vendor dragula, add one conserving action (MOVE_STORY), render placed cards, feed the pill points. Honesty banner, dependencies, exports all stay out.
- **AI contribution**:
  - **Review-then-propose** (per RoE): reviewed brief3.md against the *actual* Brief 2 code before agreeing to build, with file:line grounding. Confirmed every claim: the count-vs-points bug is real (`render.js:84` fed `placedStoryIds.length` to `pillState`, which expects points); `removeIdFromArrays` and `backlogGroups`-by-membership seams exist; dragula is at the sibling repo; the brief's asserted pill cases (16/18 neutral, 19/18 amber, 24/18 red) tie out against the real `pillState`. Pushed back on four calls, all approved: (1) carry a `beforeId` (drop sibling's story id) in the payload, not a numeric index - kills the remove-then-insert off-by-one on same-array reorder; (2) per-epic-group backlog containers with dragula `accepts` refusing cross-epic drops, not one flat container - structurally honours the epicId-invariant ruling and fits the epic-grouped render; (3) replace `epicSummary.storyCount` with `unplacedCount` outright (single consumer) rather than carry both; (4) flagged and fixed an edge the brief missed - the No-epic group must persist as a drop target while any unparented story is *placed*, else a returning loose card has nowhere to land.
  - **Build (TDD, red-green-refactor)**: pure layer first, tests watched fail before implementation. `MOVE_STORY` action + reducer (`moveStory`: removeIdFromArrays then insertBefore; rejects unknown id / out-of-range sprint by returning state unchanged; `lastReturnedStoryIds: []` on success so a manual drag fires no toast). New `board-selectors.js` (`placedPoints` core + `sprintPlacedPoints`). `epicSummary` → `unplacedCount`; `backlogGroups` No-epic persistence. 15 new tests (asserted cases 1-9 incl. conservation-under-moves and regeneration-after-real-placement via MOVE_STORY), plus two existing `storyCount` tests updated to the new contract. Then the view: `render.js` pill reads `sprintPlacedPoints` and renders placed cards (shared `storyCard` visual, epic dot on the board side); `drag.js` wires dragula (gesture-only: `accepts`/`moves`, drop reads beforeId, `drake.cancel(true)` reverts the optimistic DOM, `queueMicrotask` dispatch lets state drive the re-render); click-vs-drag via a `dragging` flag (set on `drag`, cleared on `dragend` after the trailing click is swallowed - verified dragula's `Y()` always emits `dragend` on drop and cancel alike). Vendored dragula into `plan/public/vendor/dragula/`.
  - **Verification**: 72/72 unit tests green (57 → 72); `tsc --noEmit` clean. Headless Chromium drove a real drag end-to-end: backlog→sprint lands the card and removes it from the panel; pill reads POINTS ("13 / 18" neutral, then "21 / 18" **red** after a second drop - off neutral for the first time, count would have read 1 then 2); a drag does **not** open the editor (modal count 0) while a click on a placed card **does** (modal count 1); backlog meta drops to "0 stories · 0 pts"; zero console errors.
- **Human contribution**: Reviewed brief3.md, took all four pushback recommendations ("I will take your recommendations. Proceed"), and authorised the build. Per RoE the approach was approved before any feature code.
- **Friction**: Low. Two real snags, both caught by the gates. (1) The Playwright seed read `localStorage` on first load and got null - autosave only fires on dispatch, so the initial paint never persists; fixed by nudging a setting to force a save before seeding. (2) `tsc` flagged `drake` as possibly-null after assignment from the `any`-typed global; wired the `.on` handlers through a non-null local instead. The `drake.cancel(true)`-at-drop-time reconciliation was load-bearing and unobvious, so it was verified by reading the minified dragula source (cancel is guarded by `w.dragging`, which is still true inside the synchronous `drop` emit, before `Y()` cleanup) rather than assumed.
- **Verdict**: Reviewing the brief against the live code before building, not just reading it, is what surfaced the No-epic-group-persistence hole and confirmed the count-vs-points finding was real rather than taking it on faith. TDD on the pure layer meant the conservation and points-not-count invariants were pinned before a single pixel moved; the asserted cases failing-on-count is a genuine regression tripwire, not decoration. The `beforeId`-over-index call removed a whole class of off-by-one bug that a numeric index would have invited. One feature per brief held: the banner, dependencies, and exports stayed out even though placement now unblocks all three.
- **Open housekeeping** (carried, not actioned): still owed - promote `--plum`/`--plumwash`, `#glyph-plan`, and the epic-palette tokens to shared `instrument-core`; register `plan` in the theme `manifest.mjs` + drift test. New this brief: the vendored dragula is a second copy of the suite's; a suite ticket should decide whether plan and retrospective share one vendored copy.
- **Time**: ~1 session.
- **Director sign-off** (closed 12 Jun 2026): All good. The four approved review calls all landed as built (beforeId-not-index payload; per-epic-group `accepts` for the epicId-invariant; `epicSummary` → `unplacedCount`; No-epic-group persists as a drop target); "I can explain every line" holds. No corrections issued.

### 2026-06-11 - Brief 3 authored: drag-and-drop placement (pre-build)

- **Task brief**: Check the live dev environment for Brief 2's actual state, ingest docs/build-state-after-brief2.md, and author docs/brief3.md. Planning/brief session in the Claude app (Fable), not a Claude Code build session.
- **AI contribution**: Read the full doc set against the live build (spec v0.5, screen designs v1.0, RoE, build log, both build-state handoffs, briefs 1 and 2) rather than authoring from the spec alone. Scoped Brief 3 to P0 #3 (drag-and-drop placement) per the handoff's recommendation: it is the keystone that lands stories in sprints, lights up the already-built capacity pill logic, and provides the placement the deferred dependency picker is waiting on. Surfaced one real finding from the live code and made it the brief's spine: **renderBoard computes the pill's placed figure as placedStoryIds.length (a count), but capacity is points** - numerically equal only while every sprint is empty. The brief mandates a pure sprintPlacedPoints selector and asserted case 1 is constructed so the tests fail if anyone ever feeds the pill a count again (8+8=16 neutral, +3=19 amber, +5=24 red; counts of 2/3/4 would all read neutral). Flagged seven judgement calls inside the brief as rulings (placed-is-points; move-is-remove-then-insert with id conservation; dragging never changes epicId; reorder is facilitation theatre; store-as-source-of-truth with dragula's optimistic DOM move reverted via cancel(true) before dispatch; minimal board card with no dependency visuals; backlog panel shows unplaced stories only, closing the Brief 2 storyCount-vs-unplaced housekeeping flag). Explicitly separated the two backlog-return paths (regeneration returns to top with toast vs manual drag-back at pointed position without toast) and flagged the manual-return landing position as the weakest call, most likely to be overturned. Forced the dragula container-granularity question (flat backlog container vs per-epic-group containers) into the PROPOSE step because a cross-group drop implies a forbidden reparent. Nine asserted cases including conservation-under-moves (multiset of ids across all arrays equals stories{} keys) and a regeneration-after-real-placement intersection guard.
- **Human contribution**: Directed the session (check env, ingest the handoff, create Brief 3, log the session). Rulings deliberately left open for director review: the seven flagged decisions await veto, with the manual-return landing position handed back hardest.
- **Friction**: Low, same honest caveat as the Brief 2 authoring entry: this logs an authored-but-unreviewed artefact. No code or tests ran this session; the count-vs-points finding was made by reading the handoff and render.js description, not by executing anything, so the build session should confirm it against the live file before treating it as fact. The Brief 2 director-review line in this log is still Pending and now has a sibling below; two open review gates is one more than comfortable.
- **Verdict**: Grounding the brief in the live build state paid off again - the count-vs-points latent bug is exactly the class of danger-zone defect (capacity maths) the RoE was written to catch, and it was caught at the brief stage, before the code that would expose it exists. The two-return-paths separation (system toast vs manual drag) is the kind of distinction that is cheap in a brief and expensive to untangle after a conflated implementation. One feature per brief held: the honesty banner, dependencies, and exports all stayed out despite placement making each of them buildable.
- **Director review** (closed 11 Jun 2026): All good. All seven flagged rulings survive as briefed (placed-is-points; remove-then-insert conservation; epicId drag-invariant; reorder-as-theatre; cancel-then-dispatch-then-render reconciliation; minimal board card; backlog-shows-unplaced-only). Manual-return landing position ruled as proposed: pointed position, no toast. No corrections issued; brief3.md goes to Claude Code as written.
- **Time**: ~1 short session (planning only).

### 2026-06-11 - Brief 2 build: backlog panel, epic and story cards (P0 #2)

- **Task brief**: docs/brief2.md - second vertical slice. Backlog panel with epic/story cards (create/edit/delete), grouped by epic with a No-epic bucket. Stories live in the backlog only (no placement yet). Define the epic/story schema, the 6 card actions, the pure backlog selectors, and the two editor modals. Slot into the seams Brief 1 reserved; invent no new architecture.
- **AI contribution**:
  - **Propose step**: confirmed against the live code that `applySettings` already spreads `{...state}`, so `epics`/`stories` pass through regeneration untouched and asserted case 9 holds against the *existing* `regenerate.js` (no change, just a new guard test). Proposed the schema, id strategy, action payloads, selector signatures, modal local-state approach, palette, and file layout. Two design sharpenings beyond the brief's rulings: (a) **ids minted in the action creator, not the reducer**, so `reduce` stays pure and tests assert exact state with explicit ids; (b) **colourKey assigned by the reducer** via `PALETTE[epicCount % len]` (pure rotation from state) rather than the payload. Surfaced a real finding: "6-8 hues from the instrument family" must **exclude amber/red** (reserved for capacity), so picked 8 hues clear of those bands, plum first.
  - **Build (TDD, pure-first)**: `validate.js` (3 tests), `backlog-selectors.js` (4), the 6 reducer actions in `store.js`/`actions.js` (10 tests incl. delete atomicity, both DELETE_EPIC modes, and the case-9 regeneration-conservation guard). 17 new tests; 57 total, all green. Then the view layer: `dom.js`, `modal.js` (shell + unsaved-changes guard), `backlog.js` (renders from selectors; collapse is view state), `card-editor.js`, `epic-editor.js`, wired via one delegated listener in `main.js`; `.workspace` layout wrap; panel/modal/chip/colour CSS.
  - **Verification**: typecheck clean; 57/57 tests; headless Chromium drove the real editors end-to-end (create epic -> plum; add story 5pts under it; points 0 rejected with modal held open; No-epic story; DELETE_EPIC 'delete' on a 2-child epic left the unrelated No-epic story intact) with zero console errors.
- **Human contribution**: Approved the proposal as-is ("all good"), which endorsed all six baked-in rulings and both sharpenings. (The director-review line on the authoring entry below records the formal ruling sign-off.)
- **Friction**:
  - A CSS bug the unit tests could never catch: `.new-epic-wrap { display:flex }` has equal specificity to the UA `[hidden]` rule and loaded later, so it overrode `hidden` and the inline "new epic" input showed permanently. Caught only by reading the screenshot, not by any assertion - a reminder that DOM/visual state needs an eyeball (or a Playwright visibility assert), which is why `isHidden` is now in the smoke check. Fixed with `.new-epic-wrap[hidden]{display:none}`.
  - View-layer typecheck churn: `el()` returns `HTMLElement`, so `.type = "button"` on created buttons failed (no `.type`), and `for...of` over `HTMLCollection` isn't allowed under the lib settings. Resolved with `setAttribute` and `Array.from`. Also unified the modal close path: Cancel now reuses the shell's `attemptClose` guard (one code path for Cancel/Escape/overlay) after a first draft duplicated the discard logic.
- **Verdict**: Pure-first paid off again - the danger-zone work (delete atomicity, conservation) was settled in tests before any DOM existed, and the reducer stayed pure because identity minting was pushed to the creator boundary. The one escaped defect was purely visual and outside the test net's reach; worth a standing habit of a visibility assert + screenshot for any `hidden`-toggled element. One-feature-per-brief held: no DnD, no dependencies, no placement.
- **Open housekeeping** (carried): epic-palette tokens join `--plum`/`#glyph-plan` as app-local pending promotion to instrument-core; `storyCount` vs unplaced-count in the epic row to revisit when DnD lands; sub-1280 backlog drawer still deferred.
- **Time**: ~1 session.

### 2026-06-11 - Brief 2 authored: backlog panel, epic and story cards (pre-build)

- **Task brief**: Check the live dev environment for Brief 1's actual state, ingest docs/build-state-after-brief1.md, and author docs/brief2.md. Planning/brief session in the Claude app (Fable), not a Claude Code build session.
- **AI contribution**: Read the full doc set against the live build (spec v0.5, screen designs v1.0, user journeys v1.0, RoE, build log, and the Brief 1 handoff) rather than authoring from the spec alone. Scoped Brief 2 to P0 #2 (backlog panel + epic/story cards) per the handoff's recommendation, on the rationale that it unblocks DnD (#3), gives the capacity pills meaning (#4), and is presupposed by dependencies (#5) and exports (#6). Made the call to defer the card editor's Dependencies section (Screen 2) to its own brief, with rationale recorded in the brief: the picker groups candidates by sprint location, which needs placement, which needs drag-and-drop, so building it now means building it twice. Flagged five judgement calls inside the brief (location is array membership not a field; epic colour stored as a palette key not a literal; modal/dirty state kept out of the store; new stories append to the backlog bottom; points are positive integers) as "rulings, flag if you disagree" rather than baking them silently. Wrote brief2.md in brief1's voice (READ FIRST, GOAL, RULINGS, ARCHITECTURE CONSTRAINT, SCHEMA, ACTIONS, SELECTORS, UI, OUT OF SCOPE, ASSERTED CASES, DEFINITION OF DONE, PROPOSE-first close), including nine asserted reducer/selector cases (colour-rotation wrap, add/edit/delete story, both DELETE_EPIC paths, points validation, and a regeneration-still-conserves guard against the new schema).
- **Human contribution**: Directed the session (check env, ingest the handoff, create Brief 2). Scope rulings deliberately left open for director review: the five flagged decisions await veto, and two scope edges (the sub-1280px backlog drawer and whether the Resume/New-plan prompt folds in) were handed back rather than decided.
- **Friction**: Low, and worth naming honestly. This entry logs an authored-but-unreviewed artefact: per the RoE the five rulings are the director's call, so the brief ships provisional until the review pass closes them. No code or tests ran this session, so nothing executable was verified; the brief's correctness is only as good as the next review. The append-to-bottom backlog insertion is the weakest of the flagged calls and the most likely to be overturned.
- **Verdict**: Grounding the brief in the live build state (not just the spec) and flagging-not-baking kept the director's ownership intact and caught a dependency-section build-it-twice trap before any code. The real test of this session is the upcoming review: if the briefed scope and rulings survive with few corrections, the brief was sharp; if not, that is the more useful Capstone material. One feature per brief held (DnD and dependencies stayed out).
- **Director review** (closed 11 Jun 2026): All good. All five flagged rulings survived (location-as-array-membership; colour-key-not-literal; modal state out of store; new-story append-to-bottom; positive-integer points), consistent with the "all good" proposal approval recorded in the build entry above. Both scope edges ruled as proposed: sub-1280px backlog drawer stays out; Resume/New-plan prompt remains a separate brief. No corrections issued.
- **Time**: ~1 short session (planning only).

### 2026-06-11 - Brief 1: settings strip, sprint generation, capacity maths (first code)

- **Task brief**: docs/brief1.md - first vertical slice. Settings strip drives sprint generation, rendering sprint containers with capacity pills and the month rail. No cards/DnD/exports/backlog. Pure maths + rail + regeneration built and unit-tested first; UI wired second; single store with discrete named actions.
- **AI contribution**:
  - **Propose step** (per RoE): inspected the sibling suite repos to ground the stack instead of inventing one - found vanilla JS + Express + `node --test` + zod/pino, dragula already vendored, theme synced from `/var/www/suite/shared/theme`. Proposed store shape, action list, function signatures, file layout. Verified all five worked cases tie out *before* coding, surfacing one interpretation the brief never states: sprint days are counted **inclusively** (sprint 7 = 9 days, not 8, is the only reading that makes `round(18x9/14)=12` true). Flagged it rather than silently bake it.
  - **Stack review** (director asked): recommended keeping the suite stack; added two cheap insurances - `@ts-check` + JSDoc on the logic modules, and representing calendar dates as `YYYY-MM-DD` strings/integers (never JS `Date`) to dodge timezone/DST bugs in the danger zone. Both approved.
  - **Build (TDD, red-green-refactor throughout)**: `date.js` (10 tests), `plan-maths.js` (10), `month-rail.js` (4), `regenerate.js` (5), `store.js`+`actions.js` (11). 40 tests, all asserting real values from the brief. Then the view layer (`render.js`, `main.js`), `index.html`, `plan.css`, minimal `server.js`. Synced the Instrument theme via the blessed `sync-theme.mjs`; added `#glyph-plan` (inline) and the plum accent (`plan.css`) locally per the "don't touch the suite mid-brief" decision.
  - **Verification**: typecheck gate clean; 40/40 unit tests green; headless Chromium run reproduced every worked case live (defaults -> 7 sprints/cap 18, sprint 7 `0/12` partial; vel 30 -> sprint 7 `0/17`; 1-month -> 3 sprints, sprint 3 `0/5` partial) with zero console errors.
- **Human contribution**: Approved the proposed architecture; ruled on the stack question (keep vanilla suite stack, add `@ts-check` + string-dates); approved `server.js` for parity and keeping `--plum`/`#glyph-plan` local for now.
- **Friction**:
  - `tsc` reads `tsconfig.json`, not the `jsconfig.json` I first wrote (jsconfig is editor-only) - the gate silently printed help with exit 0 until corrected. Scoped the typecheck `include` to `public/js/**` so the gate needs zero extra `@types` deps (tests are covered by *running*, `server.js` is trivial node glue).
  - Playwright's package is CommonJS; the smoke script's named `import { chromium }` failed and needed the default-import form.
- **Verdict**: Test-first on the date/capacity maths was the right call exactly where the RoE says the risk lives - every worked case passed on first implementation because the interpretation work happened in the tests, not the code. The inclusive-day question would have been a latent off-by-one bug under tests-after. Grounding the stack in the real sibling repos beat proposing from memory.
- **Open housekeeping** (logged, not silently actioned): promote `#glyph-plan` and `--plum`/`--plumwash` to the shared `instrument-core.css`/`glyphs.svg`; register `plan` as a SURFACE in the theme `manifest.mjs` and add the drift-check test; resume/new-plan prompt (Screen 3) deferred to a later brief (this slice restores the saved board silently on load).
- **Time**: ~1 session.

### 2026-06-11 - v1 screen designs and the G1-G8 rulings

- **Task brief**: Design the v1 screens (board view, card editor with dependency picker, resume/new prompt, inline setup, export dialog) consistent with suite branding; grill where the docs underdetermine the design
- **AI contribution**: Pulled live Instrument theme tokens from the retrospective repo rather than designing from brand memory; produced inline mockups of the board view and card editor; surfaced eight design gaps (G1-G8), including one the board literally cannot render without (no plan start date anywhere in the spec) and a capacity-honesty gap on partial final sprints; drafted docs/v1-screen-designs.md, the spec v0.5 errata, and this entry
- **Human contribution**: Ruled all eight in one pass, accepting the recommendations: start date defaulting to next Monday; prorated partial-sprint capacity; index-identity regeneration with displaced stories returned to backlog, never deleted; new plum accent for data-app="plan" keeping amber/red reserved for capacity semantics; Fibonacci chips plus free entry, confirmed consistent with sprintpoker's deck; optional plan title; dependency picker open to backlog stories with violations evaluated only when both sides are scheduled; board save/load kept separate from report export
- **Friction**: The missing start date survived two review passes (spec v0.3 to v0.4 and journey sign-off) because both described the month rail as "computed from sprint dates" without ever defining the anchor. Screen design forced the question; reviews of prose did not. Also: the theme-manifest.txt in the retrospective repo is stale (lists Fraunces/Inter, actual theme is Bricolage/Hanken), worth a suite housekeeping ticket
- **Verdict**: Designing against the real theme file beat designing from memory; the grill format (gap plus recommendation each) closed eight rulings in a single reply. Two forced P0 additions (start date, title) were caught and logged under the scope rule instead of sliding in silently
- **Time**: ~1 session

### 2026-06-11 - User journeys and board layout direction

- **Task brief**: Map the MVP user journeys visually (Lucid) and pressure-test the board layout against PI Program Board inspiration images
- **AI contribution**: Built two Lucid documents (E2E facilitator journey map with a layout A/B; vertical-time board sketch); challenged the SAFe-flavoured imagery against the spec's non-goals; surfaced three design holes in the proposed dependency model (month containment maths, checkbox needs a paired target, auto-grouping vs manual stack order); drafted user-journeys.md, spec v0.4 amendments, and this entry
- **Human contribution**: Chose vertical-time layout (months > sprints > stories) against the AI's column recommendation, on the Jira backlog-view familiarity argument; confirmed inspiration images as visual grammar only; accepted majority-rule month rail, either-side dependency creation, 1-to-many pairs, and tether-without-auto-move
- **Friction**: AI initially recommended horizontal time from the inspiration images and reversed when the Jira mental-model argument landed; first Lucid tool call failed on a missed approval prompt before connecting cleanly; Lucid imports leave "Text" placeholders on empty container shapes (cosmetic). Connector repair session: Lucid's edit API silently no-ops endpoint repositioning on imported lines (returned success, changed nothing) so delete-and-recreate is the reliable path; PNG export serves stale cached renders, so document data (fetch) is the verification source of truth; auto-link sometimes picks geometrically short but visually bad anchors, and explicit endpoint pinning beats it for long cross-row lines. Figma was connected and tested as an alternative but the account seat is view-only (paywalled for editing), so Lucid stays the diagram tool
- **Verdict**: Rendering the layout argument as a visual A/B in Lucid settled it faster than prose; the grilling converted a one-line dependency idea into a buildable interaction model before any code
- **Time**: ~1 session

### 2026-06-11 - Project inception (pre-code)

- **Task brief**: Evaluate the original PI Planning concept, reframe, spec the MVP, define AIDE rules of engagement
- **AI contribution**: Opportunity assessment with competitor research and scoring (18/30 as PI Planning tool, 20/30 reframed); drafted MVP spec through v0.3; drafted rules of engagement v0.1; scaffolded this /docs bundle
- **Human contribution**: Reframed the concept away from SAFe PI Planning to a capacity visualisation board; cut rooms/DM/lobby scope; decided free-to-use, single-user MVP, points-only estimation, 1-4 week sprint flexibility, three export formats, suite branding; named it sprintplan.uk and registered the domain; created the repo; flagged own scope creep (AI helper character) and parked it at P2
- **Friction**: Original concept drifted from SAFe norms (6 sprints in 3 months, parent/child dependency model); first spec draft assumed 1-2 week sprints, corrected to 1-4 weeks after the niche-workstream rationale
- **Verdict**: Reframing before building saved the project from the communication-platform trap. Spec locked before any code written
- **Time**: ~3 sessions of analysis and drafting
